[project]
name = "resource-abstractor-client"
version = "0.0.0"
dependencies = [
    "requests>=2.27.1",
    "oakestra-utils",
]

[tool.uv.sources]
oakestra-utils = { workspace = true }

[build-system]
requires = ["uv_build>=0.11.32,<0.12"]
build-backend = "uv_build"
