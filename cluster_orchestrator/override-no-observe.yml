services:

  # Remove observability services for cluster components
  cluster_alloy:
    deploy:
      mode: replicated
      replicas: 0
  cluster_loki:
    deploy:
      mode: replicated
      replicas: 0
  cluster_grafana:
    deploy:
      mode: replicated
      replicas: 0
  prometheus:
    deploy:
      mode: replicated
      replicas: 0
  node_exporter:
    deploy:
      mode: replicated
      replicas: 0
  cadvisor:
    deploy:
      mode: replicated
      replicas: 0

  docker_state_exporter:
    deploy:
      mode: replicated
      replicas: 0
