name: Python Codestyle Check (ruff)

on: pull_request

permissions:
  contents: read

jobs:
  style-check:
    uses: oakestra/.github/.github/workflows/python-codestyle.yml@v1
    with:
      # Keep in sync with the ruff rev in .pre-commit-config.yaml
      ruff_version: 0.15.22
