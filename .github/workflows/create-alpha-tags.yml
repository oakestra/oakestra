name: Create alpha tags

on:
  push:
    branches:
      - develop

permissions:
  contents: write

jobs:
  create_tag:
    uses: oakestra/.github/.github/workflows/create-tag.yml@v1
    with:
      tag_prefix: 'alpha-'
      force: true
    secrets: inherit
