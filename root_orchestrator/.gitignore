# components written locally but not checked in.
system-manager-javascript/
system-manager-quart/

# SSL certificates — never commit real certs
config/certs/*.crt
config/certs/*.key
config/certs/*.pem

