services:
  system_manager:
    volumes:
      - ./system-manager-python:/app

  cloud_scheduler:
    volumes:
      - ./cloud-scheduler:/app

  resource_abstractor:
    volumes:
      - ./resource-abstractor:/app 