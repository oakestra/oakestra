services:

  # Remove observability services for root components
  alloy:
    deploy:
      mode: replicated
      replicas: 0
  loki:
    deploy:
      mode: replicated
      replicas: 0
  grafana:
    deploy:
      mode: replicated
      replicas: 0
  prometheus:
    deploy:
      mode: replicated
      replicas: 0
  node_exporter:
    deploy:
      mode: replicated
      replicas: 0
  cadvisor:
    deploy:
      mode: replicated
      replicas: 0
