services:
  root_addons_manager:
    deploy:
      mode: replicated
      replicas: 0

  root_addons_monitor:
    deploy:
      mode: replicated
      replicas: 0

  marketplace_manager:
    deploy:
      mode: replicated
      replicas: 0

  root_addons_dashboard:
    deploy:
      mode: replicated
      replicas: 0
